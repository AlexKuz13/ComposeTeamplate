object KeystoreParamsDebug {
    const val path = "keystore-debug"
    const val storePassword = "password"
    const val keyAlias = "key-debug"
    const val keyPassword = "password"
}

object KeystoreParamsRelease {
    const val path = "keystore-release"
    const val storePassword = "password"
    const val keyAlias = "key-release"
    const val keyPassword = "password"
}